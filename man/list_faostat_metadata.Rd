% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-fao.R
\name{list_faostat_metadata}
\alias{list_faostat_metadata}
\title{List Available FAOSTAT Metadata}
\usage{
list_faostat_metadata(
  type = c("databases", "elements", "items"),
  database = NULL,
  verbose = FALSE,
  max_retries = 3
)
}
\arguments{
\item{type}{Character. Type of metadata to retrieve. Options:
\itemize{
  \item "databases": List all available FAOSTAT databases
  \item "elements": List elements (measurements) for a specific database
  \item "items": List items (products/commodities) for a specific database
}}

\item{database}{Character. Database code (required when type is "elements" or "items").
Examples: "QCL" (Crops and livestock), "RL" (Land use), "RFN" (Fertilizers).}

\item{verbose}{Logical. If TRUE, prints detailed progress messages. Default is FALSE.}

\item{max_retries}{Integer. Maximum number of retry attempts for failed requests.
Default is 3.}
}
\value{
A data.frame with metadata. Columns vary by type:
  \itemize{
    \item databases: code, label, description
    \item elements: code, label, description, unit
    \item items: code, label
  }
}
\description{
Retrieves metadata about available FAOSTAT databases, elements, and items
to help users discover what data can be fetched with \code{get_faostat_data()}.
}
\details{
This helper function makes it easier to discover what data is available in FAOSTAT
without needing to consult external documentation.

**Common FAOSTAT Databases:**
\itemize{
  \item QCL: Crops and Livestock Products
  \item RL: Land Use
  \item RFN: Fertilizers
  \item RT: Food Balance Sheets
  \item RP: Producer Prices
  \item And many more...
}

**Common Elements:**
\itemize{
  \item 2111: Stocks (for livestock)
  \item 2413: Production (for crops)
  \item 2510: Yield (for crops)
  \item 2312: Area harvested
  \item 7209: Land use area
}
}
\examples{
\dontrun{
# List all available databases
databases <- list_faostat_metadata("databases")

# List elements (measurements) for Crops and Livestock database
elements <- list_faostat_metadata("elements", database = "QCL")

# List items (products) for Crops and Livestock database
items <- list_faostat_metadata("items", database = "QCL")

# Workflow: discover what's available, then fetch data
# 1. Find database code
databases <- list_faostat_metadata("databases")
View(databases)

# 2. Find element code for production
elements <- list_faostat_metadata("elements", database = "QCL")
production_elements <- elements[grepl("production", elements$label, ignore.case = TRUE), ]

# 3. Find item code for wheat
items <- list_faostat_metadata("items", database = "QCL")
wheat <- items[grepl("wheat", items$label, ignore.case = TRUE), ]

# 4. Fetch the data
wheat_data <- get_faostat_data(
  element = "5510",
  item = "15",
  database = "QCL",
  iso3 = "USA"
)
}
}
