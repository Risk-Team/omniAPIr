% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-gee.R
\name{download_and_process_ee_image}
\alias{download_and_process_ee_image}
\title{Download and Process Google Earth Engine Image Data}
\usage{
download_and_process_ee_image(
  asset_id,
  output_filename,
  res,
  region,
  region_sf,
  crs = "EPSG:4326",
  band = NULL,
  multiply_factor = NULL,
  others = NULL,
  convert_uint8 = TRUE,
  conda_env = NULL,
  verbose = TRUE,
  max_retries = 3
)
}
\arguments{
\item{asset_id}{Either a string path to EE asset OR an ee.Image/ee.ImageCollection object}

\item{output_filename}{Character string specifying the output file path}

\item{res}{Numeric resolution in meters for the output raster}

\item{region}{Earth Engine geometry object defining the region of interest}

\item{region_sf}{sf object defining the region for cropping and masking}

\item{crs}{Character string specifying the coordinate reference system (default: "EPSG:4326")}

\item{band}{Character string or vector specifying which bands to select (optional)}

\item{multiply_factor}{Numeric factor to multiply pixel values by (optional)}

\item{others}{Function to apply additional processing to the EE object (optional)}

\item{convert_uint8}{Logical, whether to convert to 8-bit unsigned integer (default: TRUE)}

\item{conda_env}{Character string specifying the conda environment with ee and geemap packages}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}

\item{max_retries}{Numeric, maximum number of retry attempts for API calls (default: 3)}
}
\value{
A terra raster object cropped and masked to the specified region
}
\description{
Downloads raster data from Google Earth Engine (GEE) and processes it for a specific region.
Supports both ee.Image and ee.ImageCollection objects, with optional band selection,
multiplication factors, and custom processing functions.
}
\details{
This function provides a comprehensive interface for downloading and processing Google Earth Engine
raster data. It automatically handles both Image and ImageCollection objects, applies optional
transformations, and returns a processed terra raster object ready for analysis.

The function requires Python packages 'ee' (earthengine-api) and 'geemap' to be installed in
the specified conda environment. Make sure to authenticate with Google Earth Engine before use.
}
\examples{
\dontrun{
# Download Landsat 8 surface reflectance for a region
library(sf)
library(terra)

# Define region
region_sf <- st_read("study_area.shp")
region_ee <- geemap::sf_as_ee(region_sf)

# Download image
raster_data <- download_and_process_ee_image(
  asset_id = "LANDSAT/LC08/C02/T1_L2",
  output_filename = "landsat8.tif",
  res = 30,
  region = region_ee,
  region_sf = region_sf,
  band = c("SR_B4", "SR_B3", "SR_B2"),  # RGB bands
  conda_env = "gee_env"
)

# Download with custom processing
ndvi_data <- download_and_process_ee_image(
  asset_id = "LANDSAT/LC08/C02/T1_L2",
  output_filename = "ndvi.tif",
  res = 30,
  region = region_ee,
  region_sf = region_sf,
  others = function(img) {
    # Calculate NDVI
    ndvi <- img$normalizedDifference(c("SR_B5", "SR_B4"))
    return(ndvi)
  },
  conda_env = "gee_env"
)
}

}
