% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-copernicus-marine.R
\name{download_and_process_copernicus_marine}
\alias{download_and_process_copernicus_marine}
\title{Download and Process Copernicus Marine Raster Data}
\usage{
download_and_process_copernicus_marine(
  dataset_id,
  output_filename,
  output_directory = ".",
  variables = NULL,
  region_sf = NULL,
  bbox = NULL,
  start_datetime = NULL,
  end_datetime = NULL,
  minimum_depth = NULL,
  maximum_depth = NULL,
  dataset_version = NULL,
  dataset_part = NULL,
  service = NULL,
  coordinates_selection_method = "inside",
  username = NULL,
  password = NULL,
  credentials_file = NULL,
  conda_env = NULL,
  overwrite = FALSE,
  skip_existing = FALSE,
  verbose = TRUE,
  max_retries = 3,
  netcdf_compression_level = 0,
  netcdf3_compatible = FALSE,
  chunk_size_limit = -1,
  crop = TRUE,
  mask = TRUE
)
}
\arguments{
\item{dataset_id}{Character. Copernicus Marine dataset ID.}

\item{output_filename}{Character. Optional output file name. Extension is
optional; Copernicus Marine uses \code{file_format} when needed.}

\item{output_directory}{Character. Output directory. Default is the current
working directory.}

\item{variables}{Character vector. Optional variable names to extract.}

\item{region_sf}{Optional sf object. If supplied, its EPSG:4326 bounding box
is used as the Copernicus Marine subset extent.}

\item{bbox}{Optional numeric vector defining the subset extent as
\code{c(xmin, ymin, xmax, ymax)} or a named vector with names
\code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}. Ignored when
\code{region_sf} is supplied.}

\item{start_datetime, end_datetime}{Character, Date, POSIXct, or NULL.
Temporal subset bounds accepted by the Copernicus Marine Toolbox.}

\item{minimum_depth, maximum_depth}{Numeric. Optional depth bounds.}

\item{dataset_version}{Character. Optional dataset version.}

\item{dataset_part}{Character. Optional dataset part.}

\item{service}{Character. Optional Copernicus Marine service name or short
name, for example \code{"geoseries"} or \code{"timeseries"}.}

\item{coordinates_selection_method}{Character. One of \code{"inside"},
\code{"strict-inside"}, \code{"nearest"}, or \code{"outside"}.
Default is \code{"inside"}.}

\item{username, password}{Character. Copernicus Marine credentials. If NULL,
the Python toolbox falls back to environment variables or configured
credentials.}

\item{credentials_file}{Character. Optional path to a Copernicus Marine
credentials file.}

\item{conda_env}{Character. Conda environment name containing the
\code{copernicusmarine} Python package.}

\item{overwrite}{Logical. Overwrite existing output files. Default is FALSE.}

\item{skip_existing}{Logical. Skip files that already exist. Default is FALSE.}

\item{verbose}{Logical. If TRUE, prints progress messages. Default is TRUE.}

\item{max_retries}{Integer. Maximum number of retry attempts. Default is 3.}

\item{netcdf_compression_level}{Integer. NetCDF compression level from 0 to 9.
Default is 0.}

\item{netcdf3_compatible}{Logical. Request NetCDF3-compatible output.
Default is FALSE.}

\item{chunk_size_limit}{Integer. Copernicus Marine chunk size limit. Default
is -1, matching the Python toolbox default.}

\item{crop}{Logical. If TRUE and \code{region_sf} is supplied, crop the raster
to the region extent. Default is TRUE.}

\item{mask}{Logical. If TRUE and \code{region_sf} is supplied, mask the raster
to the region geometry. Default is TRUE.}
}
\value{
A terra raster object.
}
\description{
Downloads a Copernicus Marine NetCDF subset and loads it as a terra raster.
When \code{region_sf} is supplied, the raster can also be cropped and masked
to the region.
}
\examples{
\dontrun{
library(sf)
region <- st_read("study_area.shp")

temperature <- download_and_process_copernicus_marine(
  dataset_id = "cmems_mod_glo_phy-thetao_anfc_0.083deg_P1D-m",
  output_filename = "temperature.nc",
  variables = "thetao",
  region_sf = region,
  start_datetime = "2024-01-01",
  end_datetime = "2024-01-31",
  username = "your_username",
  password = "your_password",
  conda_env = "marine_env"
)
}
}
