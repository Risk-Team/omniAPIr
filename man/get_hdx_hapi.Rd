% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-miscellaneous.R
\name{get_hdx_hapi}
\alias{get_hdx_hapi}
\title{Fetch data from the HDX Humanitarian API}
\usage{
get_hdx_hapi(
  endpoint,
  ...,
  app_identifier = Sys.getenv("HDX_HAPI_APP_IDENTIFIER"),
  app_identifier_in = c("query", "header"),
  page_size = 10000,
  output_format = "json",
  max_retries = 3,
  retry_base = 2,
  base_url = "https://hapi.humdata.org"
)
}
\arguments{
\item{endpoint}{Character. HAPI endpoint path, URL, or short endpoint id.}

\item{...}{Additional scalar query parameters passed to the endpoint.}

\item{app_identifier}{Character. Base64 encoded HAPI app identifier. Defaults
to \code{Sys.getenv("HDX_HAPI_APP_IDENTIFIER")}.}

\item{app_identifier_in}{Character. Send the app identifier as a query
parameter (\code{"query"}) or as the
\code{X-HDX-HAPI-APP-IDENTIFIER} header (\code{"header"}).}

\item{page_size}{Integer. Records per HAPI page. HAPI caps this at 10,000;
this controls request size only, not the number of rows returned.}

\item{output_format}{Character. HAPI output format. Defaults to \code{"json"}.}

\item{max_retries}{Integer. Maximum retry attempts for HTTP 429 and 5xx
responses.}

\item{retry_base}{Numeric. Exponential backoff base in seconds.}

\item{base_url}{Character. HAPI base URL.}
}
\value{
A tibble containing rows from the HAPI \code{data} array.
}
\description{
Low-level helper for HDX HAPI endpoints. This uses the HDX Humanitarian API,
not the generic HDX CKAN API. It accepts either a full endpoint path such as
\code{"/api/v2/metadata/data-availability"} or a short endpoint id such as
\code{"availability"} or \code{"wfp_prices"}.
}
\examples{
\dontrun{
availability <- get_hdx_hapi("availability", location_code = "KEN")
prices <- get_hdx_hapi("wfp_prices", location_code = "KEN", page_size = 1000)
}
}
