% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-biodiversity.R
\name{get_invasive_alien_species}
\alias{get_invasive_alien_species}
\title{Get Invasive Alien Species Data}
\usage{
get_invasive_alien_species(
  iso3,
  max_species = 200,
  limit_per_species = 1000,
  output_filename,
  geolocation = FALSE,
  verbose = FALSE,
  max_retries = 3
)
}
\arguments{
\item{iso3}{Character. ISO3 country code (required).}

\item{max_species}{Integer. Maximum number of species to retrieve. Default is 200.}

\item{limit_per_species}{Integer. Maximum occurrences per species when
geolocation is TRUE. Default is 1000.}

\item{output_filename}{Character. Path for saving the downloaded DwC-A archive.}

\item{geolocation}{Logical. If TRUE, fetches occurrence data with coordinates.
Default is FALSE.}

\item{verbose}{Logical. If TRUE, prints detailed progress messages. Default is FALSE.}

\item{max_retries}{Integer. Maximum number of retry attempts for failed requests.
Default is 3.}
}
\value{
A data.frame or sf object (if geolocation=TRUE) containing invasive
  species data with taxonomy and occurrence information.
}
\description{
Retrieves invasive and alien species data from the Global Register of
Introduced and Invasive Species (GRIIS) via GBIF API. Optionally fetches
species occurrence data with geolocation.
}
\details{
API Documentation: \url{https://www.gbif.org/developer/summary}

The function downloads the GRIIS dataset for the specified country, identifies
alien and invasive species, and optionally retrieves occurrence data with
coordinates from GBIF.
}
\examples{
\dontrun{
# Get invasive species list only
invasive_spp <- get_invasive_alien_species(
  iso3 = "KEN",
  output_filename = "griis_kenya.zip"
)

# Get invasive species with locations
invasive_spp_geo <- get_invasive_alien_species(
  iso3 = "KEN",
  output_filename = "griis_kenya.zip",
  geolocation = TRUE
)
}
}
